import { createMetadata } from "@/lib/metadata";
export const metadata = createMetadata({ title: "Services", description: "Browse our professional digital services — Discord servers, websites, streaming overlays, mobile apps and more.", path: "/services" });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
