import { createMetadata } from "@/lib/metadata";
export const metadata = createMetadata({ title: "Plans", description: "Monthly subscription plans for creators and businesses. Get ongoing design, SEO, copywriting, and creative support from $29/month. Cancel anytime.", path: "/plans" });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
